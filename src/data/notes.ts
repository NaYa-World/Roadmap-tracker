import type { NotesMap } from '../types';

export const NOTES: NotesMap = {
  p0d0: {
    phase: 'Phase 1 — Foundations',
    day:   'Day 1 — How The Web Works',
    sections: [
      { type: 'h2', t: 'What Happens When You Type a URL' },
      { type: 'p',  t: 'Every website visit is a <strong>6-step journey</strong> that completes in under a second. Understanding this separates a developer from someone who just copies code.' },
      { type: 'h3', t: 'Step 1 — DNS Lookup' },
      { type: 'p',  t: 'Your browser does not understand <strong>google.com</strong>. DNS (Domain Name System) acts as the internet\'s phonebook and translates it to an IP address like <strong>142.250.195.46</strong>.' },
      { type: 'code', t: 'You type:       google.com\nDNS translates: google.com → 142.250.195.46\nBrowser:        connects to 142.250.195.46' },
      { type: 'tip', t: '<strong>Try it now:</strong> Open terminal and run <code>nslookup google.com</code>. You see DNS doing its job live.' },
      { type: 'h3', t: 'Steps 2–6 — TCP → TLS → Request → Response → Render' },
      { type: 'code', t: '// Step 2: TCP connection to port 443\n// Step 3: TLS handshake = the padlock icon\n// Step 4: Browser sends HTTP Request\nGET / HTTP/1.1\nHost: google.com\n\n// Step 5: Server responds\nHTTP/1.1 200 OK\nContent-Type: text/html\n\n// Step 6: Browser builds DOM, runs CSS + JS, paints screen' },
      { type: 'warn', t: '<strong>Never enter passwords on HTTP sites (no padlock).</strong> Anyone on the same WiFi can read your data in plain text.' },
      { type: 'h2', t: 'HTTP Status Codes' },
      { type: 'table', heads: ['Code', 'Meaning', 'When You See It'], rows: [
        ['200', 'OK — success', 'Page loaded fine'],
        ['201', 'Created', 'After submitting a form'],
        ['301', 'Redirect permanently', 'Old URL moved to new'],
        ['400', 'Bad Request', 'You sent wrong data'],
        ['401', 'Unauthorised', 'Not logged in'],
        ['403', 'Forbidden', 'Logged in but no permission'],
        ['404', 'Not Found', 'Broken link / wrong URL'],
        ['500', 'Server Error', 'Bug / crash on server'],
      ]},
      { type: 'tip', t: '<strong>Memory trick:</strong> 2xx = Success · 3xx = Redirect · 4xx = Your fault · 5xx = Server\'s fault' },
      { type: 'h2', t: 'GET vs POST' },
      { type: 'code', t: '// GET — data visible in URL (bookmarkable, cacheable)\nGET /search?q=javascript HTTP/1.1\n\n// POST — data in body (hidden from URL)\nPOST /login HTTP/1.1\nContent-Type: application/json\n{ "email": "gk@email.com", "password": "secret" }' },
      { type: 'warn', t: '<strong>Never use GET for passwords.</strong> The URL is stored in browser history, server logs, and ISP logs.' },
      { type: 'h2', t: 'What is the DOM?' },
      { type: 'p', t: 'When the browser reads HTML, it builds a <strong>live tree in memory</strong> called the DOM. JavaScript reads and modifies this tree to make pages interactive.' },
      { type: 'code', t: 'HTML file (static text on disk):\n<h1>Hello</h1>\n\nDOM in browser memory (live tree):\ndocument → html → body → h1 → "Hello"\n\n// JS modifies DOM live:\ndocument.querySelector("h1").textContent = "Changed!"' },
      { type: 'tip', t: '<strong>Try this:</strong> Open any website → F12 → Console → type: document.querySelector("h1").style.color = "red". The heading turns red. That\'s the DOM. Refresh to undo.' },
      { type: 'h2', t: '💡 Senior Developer Suggestions' },
      { type: 'tip', t: '<strong>Bookmark MDN immediately.</strong> developer.mozilla.org is your bible — accurate, up to date, maintained by Mozilla. Not W3Schools, not random blogs.' },
      { type: 'tip', t: '<strong>Network tab solves 70% of bugs.</strong> Before asking "why isn\'t my data loading?" open DevTools → Network. Check status code, check response body. The answer is almost always there.' },
      { type: 'tip', t: '<strong>At 33, your maturity is an asset.</strong> You understand deadlines and professional communication. Most fresh graduates don\'t. Use this in interviews — give real-world analogies.' },
      { type: 'h2', t: '🔑 Key Points — Day 1' },
      { type: 'keypoints', items: [
        '<strong>6 steps:</strong> DNS → TCP → TLS → HTTP Request → HTTP Response → Browser Renders',
        '<strong>DNS</strong> converts domain names (google.com) into IP addresses computers understand',
        '<strong>HTTPS = HTTP + TLS encryption.</strong> The padlock = safe. Never send passwords over HTTP',
        '<strong>2xx = success · 3xx = redirect · 4xx = your fault · 5xx = server fault</strong>',
        '<strong>GET</strong> = fetch (visible in URL). <strong>POST</strong> = send data (body, hidden). Never GET passwords',
        '<strong>DOM</strong> is a live in-memory tree. JavaScript modifies it to make pages interactive',
        '<strong>CDN</strong> serves your site from the nearest server globally — Vercel gives you this free',
        '<strong>DevTools (F12)</strong> is your most important tool. Use it every single day',
      ]},
      { type: 'h2', t: '📝 Quiz — Answer Before Moving to Day 2' },
      { type: 'quiz', qs: [
        'A user gets a 403 error. What does that mean and where do you look?',
        'You\'re building a payment form. GET or POST? Why exactly?',
        'What is the difference between the HTML file on disk and the DOM?',
        'Your site loads in 3s for US users but 9s for Indian users. Cause and fix?',
        'Open DevTools → Network tab on any site. Find one API call and describe its status code.',
      ]},
    ],
  },
  p0d1: {
    phase: 'Phase 1 — Foundations',
    day:   'Day 2 — HTML5 Deep Dive',
    sections: [
      { type: 'h2', t: 'Why Semantic HTML Matters' },
      { type: 'p', t: 'Semantic HTML uses tags that <strong>describe the meaning</strong> of content — not just how it looks. Screen readers use them for visually impaired users. Google\'s crawler uses them for SEO.' },
      { type: 'code', t: '<!-- BAD — no meaning -->\n<div class="header"><div class="nav">...</div></div>\n\n<!-- GOOD — semantic -->\n<header><nav>...</nav></header>\n<main><article>...</article></main>\n<aside>sidebar</aside>\n<footer>footer</footer>' },
      { type: 'h2', t: 'Core Semantic Tags' },
      { type: 'table', heads: ['Tag', 'Purpose', 'Example'], rows: [
        ['<header>', 'Page/section header', 'Logo, nav'],
        ['<nav>', 'Navigation links', 'Main menu'],
        ['<main>', 'Primary content (one per page)', 'Page body'],
        ['<section>', 'Thematic group', 'Feature sections'],
        ['<article>', 'Self-contained content', 'Blog post'],
        ['<aside>', 'Secondary content', 'Sidebar'],
        ['<footer>', 'Bottom of page', 'Links, copyright'],
      ]},
      { type: 'h2', t: 'Forms — The Most Important HTML Element' },
      { type: 'code', t: '<form action="/submit" method="POST">\n  <label for="name">Full Name</label>\n  <input type="text" id="name" name="name" required minlength="2"/>\n\n  <input type="email" name="email" required/>\n  <input type="password" name="password" minlength="8"/>\n  <input type="number" name="age" min="18" max="100"/>\n\n  <select name="city">\n    <option value="hyd">Hyderabad</option>\n    <option value="blr">Bangalore</option>\n  </select>\n\n  <input type="checkbox" id="agree" name="agree" required/>\n  <label for="agree">I agree to terms</label>\n\n  <button type="submit">Submit</button>\n</form>' },
      { type: 'h2', t: 'Accessibility Basics' },
      { type: 'code', t: '<!-- Images: always describe content -->\n<img src="profile.jpg" alt="GK profile photo"/>\n<!-- Decorative: empty alt -->\n<img src="divider.png" alt=""/>\n\n<!-- ARIA roles -->\n<button aria-label="Close dialog">✕</button>\n<div role="alert">Error: Please fill all fields</div>' },
      { type: 'warn', t: '<strong>Client-side validation is not enough.</strong> Always validate on the server too. Anyone can disable HTML validation in DevTools.' },
      { type: 'h2', t: '🔑 Key Points — Day 2' },
      { type: 'keypoints', items: [
        '<strong>Semantic HTML</strong> uses meaningful tags (header, main, article) — better SEO and accessibility',
        '<strong>Every image needs alt text</strong> — for screen readers and when image fails to load',
        '<strong>Every input needs a label</strong> — for= must match the input\'s id=',
        '<strong>Use correct input types</strong> — email, password, number give free validation + correct mobile keyboards',
        '<strong>form method="POST"</strong> hides data from URL — always use for passwords',
        '<strong>required attribute</strong> is client-side only — always also validate on the server',
      ]},
      { type: 'quiz', qs: [
        'Why use <article> instead of <div class="article">? What\'s the actual benefit?',
        'What happens if you forget the alt attribute on an image?',
        'A login form — which method attribute value and which HTML attributes enforce security?',
        'What is the purpose of aria-label? Give a real example.',
      ]},
    ],
  },
  p0d2: {
    phase: 'Phase 1 — Foundations',
    day:   'Day 3 — CSS Fundamentals',
    sections: [
      { type: 'h2', t: 'The Box Model — Everything is a Box' },
      { type: 'code', t: '.element {\n  /* Inside → outside */\n  padding: 16px;    /* space INSIDE border */\n  border: 1px solid #ccc;\n  margin: 24px;     /* space OUTSIDE border */\n}\n\n/* ALWAYS add this first line in every project */\n* { box-sizing: border-box; }\n/* Without: width = content only (padding makes it wider!) */\n/* With: width INCLUDES padding + border */' },
      { type: 'tip', t: '<strong>Always add * { box-sizing: border-box; } as your first CSS line.</strong> Without it, adding padding makes elements wider than you set. This trips up every beginner.' },
      { type: 'h2', t: 'Selectors and Specificity' },
      { type: 'code', t: '/* Specificity (low → high): */\n*         { }  /* 0 — universal */\np         { }  /* 1 — element */\n.card     { }  /* 10 — class */\n#header   { }  /* 100 — ID */\n[style=""]     /* 1000 — inline */\n\n/* Higher specificity always wins conflicts */\n.card p { color: blue; }  /* specificity: 11 */\n#hero p  { color: red;  }  /* specificity: 101 — WINS */' },
      { type: 'h2', t: 'Positioning' },
      { type: 'code', t: '.parent { position: relative; }\n.tooltip {\n  position: absolute; /* relative to nearest non-static parent */\n  top: -40px;\n  left: 0;\n}\n\n.sticky-nav {\n  position: sticky;\n  top: 0;       /* sticks when scrolled to top */\n  z-index: 100;\n}' },
      { type: 'tip', t: '<strong>Most layout bugs come from misunderstanding position: absolute.</strong> It positions relative to the nearest ancestor with position: relative. If none, it uses the viewport.' },
      { type: 'h2', t: '🔑 Key Points — Day 3' },
      { type: 'keypoints', items: [
        '<strong>Box model:</strong> content → padding → border → margin. Always add box-sizing: border-box',
        '<strong>Specificity:</strong> inline > ID > class > element. Higher specificity always wins',
        '<strong>position: relative</strong> offsets from normal position. <strong>absolute</strong> positions to nearest non-static parent',
        '<strong>position: sticky</strong> acts relative until scroll threshold then becomes fixed — perfect for navbars',
        '<strong>Use class selectors</strong> for CSS, not ID selectors — high specificity causes hard-to-fix conflicts',
        '<strong>display: none</strong> removes from layout entirely. <strong>visibility: hidden</strong> hides but keeps space',
      ]},
      { type: 'quiz', qs: [
        'What is the difference between margin and padding?',
        'You have position: absolute. What does it position relative to?',
        'Two rules: .card p {} and #hero p {}. Which wins and why?',
        'display: none vs visibility: hidden — what is the difference?',
      ]},
    ],
  },
  p0d3: {
    phase: 'Phase 1 — Foundations',
    day:   'Day 4 — CSS Flexbox',
    sections: [
      { type: 'h2', t: 'Container Properties' },
      { type: 'code', t: '.container {\n  display: flex;\n  flex-direction: row;           /* left → right (default) */\n  flex-direction: column;        /* top → bottom */\n  justify-content: center;       /* main axis alignment */\n  justify-content: space-between;/* gaps between items */\n  align-items: center;           /* cross axis alignment */\n  flex-wrap: wrap;               /* allow wrapping */\n  gap: 16px;                     /* space between items */\n}' },
      { type: 'h2', t: 'Children Properties' },
      { type: 'code', t: '.item {\n  flex: 1;         /* grow and fill available space equally */\n  flex: 0 0 200px; /* fixed 200px, no grow, no shrink */\n  align-self: center; /* override container align-items */\n}' },
      { type: 'h2', t: 'Most Common Patterns' },
      { type: 'code', t: '/* Centre anything — most asked interview question */\n.centre {\n  display: flex;\n  justify-content: center;\n  align-items: center;\n}\n\n/* Navbar: logo left, links right */\n.navbar {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n}\n\n/* Responsive card grid */\n.grid {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 20px;\n}\n.card {\n  flex: 1 1 300px; /* grow, shrink, min 300px */\n  max-width: 400px;\n}' },
      { type: 'tip', t: '<strong>justify-content works on main axis, align-items on cross axis.</strong> In flex-direction: row (default), main = horizontal, cross = vertical. In column, they flip.' },
      { type: 'h2', t: '🔑 Key Points — Day 4' },
      { type: 'keypoints', items: [
        '<strong>display: flex</strong> on the parent activates flexbox for all direct children',
        '<strong>justify-content</strong> aligns on main axis (horizontal in row mode)',
        '<strong>align-items</strong> aligns on cross axis (vertical in row mode)',
        '<strong>gap</strong> creates space between items — always prefer over margin hacks',
        '<strong>flex: 1</strong> makes children share available space equally',
        '<strong>flex-wrap: wrap</strong> lets items wrap to next line when space runs out',
        'To centre anything: display:flex + justify-content:center + align-items:center',
      ]},
      { type: 'quiz', qs: [
        'What is the difference between justify-content and align-items?',
        'How do you centre a div both horizontally and vertically using flexbox?',
        'What does flex: 1 mean? Write it in long form (grow shrink basis).',
        'Write CSS for a navbar with logo on left and links on right.',
      ]},
    ],
  },
  p0d4: {
    phase: 'Phase 1 — Foundations',
    day:   'Day 5 — CSS Grid + Responsive Design',
    sections: [
      { type: 'h2', t: 'CSS Grid — 2D Layouts' },
      { type: 'code', t: '.grid {\n  display: grid;\n  grid-template-columns: repeat(3, 1fr);  /* 3 equal columns */\n  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); /* responsive! */\n  gap: 20px;\n}\n\n.hero    { grid-column: 1 / -1; }  /* span all columns */\n.sidebar { grid-column: 1; grid-row: 2 / 4; }' },
      { type: 'tip', t: '<strong>repeat(auto-fit, minmax(280px, 1fr))</strong> is the most powerful responsive pattern. No media queries needed — automatically creates as many columns as fit.' },
      { type: 'h2', t: 'Media Queries — Mobile First' },
      { type: 'code', t: '/* Mobile FIRST — start with mobile styles */\n.container { padding: 16px; font-size: 14px; }\n\n/* Enhance for larger screens with min-width */\n@media (min-width: 768px) {\n  .container { padding: 32px; font-size: 16px; }\n}\n@media (min-width: 1024px) {\n  .container { max-width: 1200px; margin: 0 auto; }\n}' },
      { type: 'h2', t: 'CSS Custom Properties' },
      { type: 'code', t: ':root {\n  --color-primary: #7864ff;\n  --spacing-md: 16px;\n  --border-radius: 8px;\n}\n\n.button {\n  background: var(--color-primary);\n  padding: var(--spacing-md);\n}\n\n/* Dark mode override */\n[data-theme="dark"] {\n  --color-primary: #a090ff;\n}' },
      { type: 'h2', t: '🔑 Key Points — Day 5' },
      { type: 'keypoints', items: [
        '<strong>Grid = 2D (rows AND columns).</strong> Flexbox = 1D (row OR column)',
        '<strong>repeat(auto-fit, minmax(280px, 1fr))</strong> — memorise this, it\'s your most-used pattern',
        '<strong>Mobile first</strong> — write mobile styles first, use min-width media queries for larger screens',
        '<strong>CSS variables (--name: value)</strong> in :root make theming and dark mode trivial',
        '<strong>fr unit</strong> = fraction of available space. 1fr 1fr = two equal columns',
        '<strong>gap</strong> works on both Grid and Flexbox',
      ]},
      { type: 'quiz', qs: [
        'When would you use Grid over Flexbox? Give one example of each.',
        'Write CSS for a responsive card grid: 1 col mobile, 2 tablet, 3 desktop.',
        'What is mobile-first design? Why is min-width better than max-width?',
        'How do CSS variables work? Write a dark mode example using data-theme.',
      ]},
    ],
  },
  p1d0: {
    phase: 'Phase 2 — TypeScript',
    day:   'Day 11 — Why TypeScript Exists',
    sections: [
      { type: 'h2', t: 'The Problem TypeScript Solves' },
      { type: 'code', t: '// JavaScript — fails silently at RUNTIME\nfunction greet(user) {\n  return "Hello " + user.nane; // typo: nane not name\n  // Returns "Hello undefined" — no error!\n}\n\n// TypeScript — caught at COMPILE time\nfunction greet(user: { name: string }) {\n  return "Hello " + user.nane;\n  // ERROR: Property "nane" does not exist\n  // TS refuses to compile — you fix it before shipping\n}' },
      { type: 'h2', t: 'Setup' },
      { type: 'code', t: 'npm install -g typescript\ntsc --init  # creates tsconfig.json\ntsc --watch # re-compiles on save\n\n// tsconfig.json essentials:\n{\n  "compilerOptions": {\n    "target": "ES2020",\n    "strict": true,   // ALWAYS enable this\n    "outDir": "./dist",\n    "rootDir": "./src"\n  }\n}' },
      { type: 'tip', t: '<strong>Always enable strict: true.</strong> Without it, TypeScript is barely better than JavaScript. With it, it catches almost every runtime bug at compile time.' },
      { type: 'h2', t: '🔑 Key Points — Day 11' },
      { type: 'keypoints', items: [
        '<strong>TypeScript is a superset of JavaScript</strong> — all valid JS is valid TS',
        '<strong>Compiles to plain JavaScript</strong> — browsers only run JS, never TS',
        '<strong>strict: true is non-negotiable</strong> — enables all checks that make TS valuable',
        '<strong>Type annotations</strong> tell TS what type a variable or parameter must be',
        '<strong>VS Code has built-in TS support</strong> — hover any variable to see its inferred type',
      ]},
      { type: 'quiz', qs: [
        'What problem does TypeScript solve that JavaScript doesn\'t?',
        'TypeScript compiles to what? What does the browser actually run?',
        'What does strict: true enable? Why is it important?',
      ]},
    ],
  },
  p1d1: {
    phase: 'Phase 2 — TypeScript',
    day:   'Day 12 — Types and Type Inference',
    sections: [
      { type: 'h2', t: 'Primitive Types' },
      { type: 'code', t: 'let name: string = "GK";\nlet age: number = 33;\nlet hired: boolean = false;\nlet nothing: null = null;\nlet notSet: undefined = undefined;\n\n// Special types\nlet safe: unknown = getData(); // must narrow before using\nlet bad: any = "avoid this";  // turns off type checking\n\n// Type inference — TS figures these out automatically\nlet city = "Hyderabad"; // TS infers: string\nlet count = 0;          // TS infers: number' },
      { type: 'h2', t: 'Union and Literal Types' },
      { type: 'code', t: '// Union — can be one of several types\nlet id: string | number;\nid = "123"; // ok\nid = 123;   // ok\nid = true;  // ERROR\n\n// Literal types — exact values only\ntype Status = "idle" | "loading" | "success" | "error";\ntype Direction = "north" | "south" | "east" | "west";\n\n// Type narrowing\nfunction process(val: string | number) {\n  if (typeof val === "string") {\n    return val.toUpperCase(); // TS knows it\'s string here\n  }\n  return val * 2; // TS knows it\'s number here\n}' },
      { type: 'h2', t: '🔑 Key Points — Day 12' },
      { type: 'keypoints', items: [
        '<strong>Annotate parameters and return types</strong> — TS can infer variables but not function parameters',
        '<strong>Never use any</strong> — it defeats TypeScript entirely. Use unknown instead',
        '<strong>unknown is safe</strong> — you must check the type before using it. any is not safe',
        '<strong>Union types (A | B)</strong> allow a value to be one of several types',
        '<strong>Literal types</strong> restrict a value to specific allowed values',
        '<strong>Type narrowing</strong> — typeof/instanceof checks tell TS which type you have in that block',
      ]},
      { type: 'quiz', qs: [
        'What is the difference between any and unknown?',
        'When does TypeScript infer types and when must you annotate explicitly?',
        'Write a function that accepts string or number and returns length/doubled value.',
        'What are literal types? Give a real example.',
      ]},
    ],
  },
  p1d2: {
    phase: 'Phase 2 — TypeScript',
    day:   'Day 13 — Interfaces and Type Aliases',
    sections: [
      { type: 'h2', t: 'Interface' },
      { type: 'code', t: 'interface User {\n  id: number;\n  name: string;\n  email: string;\n  age?: number;            // optional\n  readonly createdAt: Date; // cannot change after creation\n}\n\n// Extending\ninterface AdminUser extends User {\n  permissions: string[];\n}' },
      { type: 'h2', t: 'Type Alias vs Interface' },
      { type: 'code', t: '// type alias — more flexible\ntype ID = string | number;       // union\ntype Callback = () => void;      // function type\n\n// interface — objects only, but extensible\ninterface Point { x: number; y: number; }\n\n// Intersection types\ntype AdminUser = User & { permissions: string[] };\n\n// Rule: interface for object shapes, type for everything else' },
      { type: 'h2', t: '🔑 Key Points — Day 13' },
      { type: 'keypoints', items: [
        '<strong>interface</strong> defines object shapes — use for component props and data models',
        '<strong>?</strong> makes property optional. <strong>readonly</strong> prevents reassignment',
        '<strong>extends</strong> creates a new interface inheriting all properties',
        '<strong>type alias</strong> can represent unions, primitives, functions, objects',
        '<strong>Intersection (&)</strong> combines two types — object must satisfy both',
      ]},
      { type: 'quiz', qs: [
        'Write an interface for a Product with id, name, price, and optional description.',
        'What is the difference between interface and type? When use each?',
        'What does readonly mean? When would you use it?',
        'How do you extend an interface? Write an example.',
      ]},
    ],
  },
  p1d3: {
    phase: 'Phase 2 — TypeScript',
    day:   'Day 14–17 — Generics + Utility Types',
    sections: [
      { type: 'h2', t: 'Why Generics Exist' },
      { type: 'code', t: '// Without generics — separate function per type (bad)\nfunction getFirstString(arr: string[]): string { return arr[0]; }\nfunction getFirstNumber(arr: number[]): number { return arr[0]; }\n\n// With generics — one function, any type, type-safe\nfunction getFirst<T>(arr: T[]): T { return arr[0]; }\n\ngetFirst(["a","b","c"]); // T inferred as string\ngetFirst([1, 2, 3]);     // T inferred as number' },
      { type: 'h2', t: 'Utility Types — Memorise These' },
      { type: 'code', t: 'interface User {\n  id: number;\n  name: string;\n  email: string;\n  password: string;\n}\n\ntype UpdateUser  = Partial<User>;          // all optional\ntype ImmUser     = Readonly<User>;         // no reassignment\ntype PublicUser  = Pick<User, "id"|"name">; // keep only these\ntype SafeUser    = Omit<User, "password">; // remove password\ntype UserMap     = Record<string, User>;   // typed dictionary' },
      { type: 'tip', t: '<strong>Omit<User, "password"> is the most important utility type for web dev.</strong> When returning user data from an API, you never want to expose the password.' },
      { type: 'h2', t: '🔑 Key Points — Day 14–17' },
      { type: 'keypoints', items: [
        '<strong>Generics</strong> write functions/types that work with any type while keeping type safety',
        '<strong>T extends SomeType</strong> constrains what T can be',
        '<strong>Partial<T></strong> makes all properties optional — for update/patch operations',
        '<strong>Pick<T,K></strong> keeps only listed keys. <strong>Omit<T,K></strong> removes listed keys',
        '<strong>Record<K,V></strong> is a typed dictionary — keys are K, values are V',
      ]},
      { type: 'quiz', qs: [
        'Write a generic identity<T>(val: T): T function.',
        'What does Omit<User, "password"> produce? When use it in a real API?',
        'Difference between Partial<T> and Required<T>?',
        'Write getProperty<T, K extends keyof T>(obj: T, key: K) and explain it.',
      ]},
    ],
  },
  p2d0: {
    phase: 'Phase 3 — React + Tailwind',
    day:   'Days 26–45 — React + Tailwind + Next.js',
    sections: [
      { type: 'h2', t: 'What React Solves' },
      { type: 'code', t: '// Without React — manual DOM sync (painful)\ndocument.getElementById("count").textContent = count;\n// You must manually update DOM every time count changes\n\n// With React — declarative UI\nfunction Counter() {\n  const [count, setCount] = useState(0);\n  return (\n    <button onClick={() => setCount(c => c + 1)}>\n      Count: {count}  {/* auto-updates */}\n    </button>\n  );\n}' },
      { type: 'h2', t: 'JSX Rules' },
      { type: 'code', t: '// JSX compiles to React.createElement()\n// Rules:\n// 1. Use className not class\n// 2. Use htmlFor not for\n// 3. Self-close all tags: <br /> <img />\n// 4. Return one root (or use <> fragments)\n// 5. JS expressions go inside {}\n\nfunction Card({ name, age }: { name: string; age: number }) {\n  return (\n    <>\n      <h1>Hello {name}</h1>\n      {age >= 18 && <p>Adult</p>}\n    </>\n  );\n}' },
      { type: 'h2', t: 'Hooks Cheat Sheet' },
      { type: 'code', t: '// useState — local state\nconst [count, setCount] = useState<number>(0);\n\n// useEffect — side effects\nuseEffect(() => {\n  const sub = subscribe(id);\n  return () => sub.unsubscribe(); // cleanup!\n}, [id]); // runs when id changes\n\n// useReducer — complex state\nconst [state, dispatch] = useReducer(reducer, initialState);\n\n// useContext — global state without prop drilling\nconst theme = useContext(ThemeContext);\n\n// useMemo — memoize expensive value\nconst result = useMemo(() => heavyCalc(a, b), [a, b]);\n\n// useCallback — memoize function reference\nconst handler = useCallback(() => doThing(id), [id]);' },
      { type: 'h2', t: 'Tailwind Core Patterns' },
      { type: 'code', t: '// Responsive prefixes\n<div className="p-4 md:p-8 lg:p-12">\n\n// State variants\n<button className="bg-blue-500 hover:bg-blue-600 active:scale-95">\n\n// Dark mode\n<div className="bg-white dark:bg-gray-900">\n\n// cn() utility — conditional classes\nimport { clsx } from "clsx";\nimport { twMerge } from "tailwind-merge";\nconst cn = (...inputs) => twMerge(clsx(inputs));\n\n<div className={cn("base-class", isActive && "text-blue-500")}>' },
      { type: 'h2', t: '🔑 Key Points — React Phase' },
      { type: 'keypoints', items: [
        '<strong>React components</strong> are JS functions that return JSX — capitalise the name',
        '<strong>useState</strong> triggers re-render when called. <strong>useEffect</strong> runs after render',
        '<strong>Always return cleanup</strong> from useEffect when subscribing — prevents memory leaks',
        '<strong>useMemo</strong> memoizes values, <strong>useCallback</strong> memoizes functions',
        '<strong>Tailwind utilities</strong> — sm: md: lg: for responsive, hover: focus: for states',
        '<strong>TanStack Query</strong> for server state, <strong>Zustand</strong> for client state',
        '<strong>Zod + React Hook Form</strong> = the best form validation combo in 2025',
        '<strong>Next.js App Router</strong> — Server Components by default, add "use client" only when needed',
      ]},
      { type: 'quiz', qs: [
        'What problem does React solve that plain JS doesn\'t?',
        'When does a component re-render? List all triggers.',
        'useMemo vs useCallback — what does each memoize?',
        'When should you add "use client" in Next.js App Router?',
      ]},
    ],
  },
  p3d0: {
    phase: 'Phase 4 — DevOps + Cloud',
    day:   'Day 46 — Linux + AWS',
    sections: [
      { type: 'h2', t: 'Why Developers Must Know Linux' },
      { type: 'p', t: 'Every server your code runs on is Linux. AWS EC2, Google Cloud, Azure VMs — all Linux. You must be able to navigate a server, check logs, and deploy without a GUI.' },
      { type: 'code', t: '# Navigation\npwd && ls -la && cd /home/ubuntu\n\n# Files\nmkdir project && touch app.js && rm -rf node_modules/\n\n# Logs — your most-used command in production\ntail -f /var/log/app.log   # watch live\ngrep "ERROR" app.log       # search for errors\ngrep -r "TODO" ./src       # search recursively\n\n# Permissions\nchmod 755 script.sh  # owner: rwx, group+others: rx\nchown ubuntu file.txt' },
      { type: 'h2', t: 'SSH' },
      { type: 'code', t: '# Generate SSH key pair\nssh-keygen -t ed25519 -C "your@email.com"\n# Private key: ~/.ssh/id_ed25519 (NEVER share)\n# Public key: id_ed25519.pub (goes on server)\n\n# Connect to EC2\nssh -i ~/.ssh/my-key.pem ubuntu@12.34.56.78' },
      { type: 'warn', t: '<strong>Never run rm -rf without checking pwd first.</strong> There is no recycle bin on Linux.' },
      { type: 'h2', t: '🔑 Key Points — Day 46' },
      { type: 'keypoints', items: [
        '<strong>Every production server is Linux</strong> — this is non-negotiable for any dev role',
        '<strong>tail -f</strong> watches a log file live — first tool to use when something breaks',
        '<strong>grep</strong> searches file contents — essential for finding errors in logs',
        '<strong>chmod 755</strong> = owner: read/write/execute, group+others: read/execute',
        '<strong>SSH private key</strong> stays on your machine. Public key goes on the server',
      ]},
      { type: 'quiz', qs: [
        'What does chmod 755 mean in terms of permissions?',
        'How do you watch a log file update in real time?',
        'What is an SSH key pair? Which part goes on the server?',
        'What does grep -r "error" ./logs do?',
      ]},
    ],
  },
  p3d1: {
    phase: 'Phase 4 — DevOps + Cloud',
    day:   'Day 47–48 — Docker',
    sections: [
      { type: 'h2', t: 'Container vs VM' },
      { type: 'p', t: 'A VM emulates an entire computer + OS. A container shares the host OS kernel and only packages your app and its dependencies — lightweight, fast to start.' },
      { type: 'h2', t: 'Dockerfile' },
      { type: 'code', t: 'FROM node:20-alpine\nWORKDIR /app\nCOPY package*.json ./ # copy first for caching\nRUN npm ci\nCOPY . .\nRUN npm run build\nEXPOSE 3000\nCMD ["node", "dist/index.js"]' },
      { type: 'h2', t: 'Docker Compose' },
      { type: 'code', t: 'version: "3.8"\nservices:\n  app:\n    build: .\n    ports: ["3000:3000"]\n    environment:\n      DATABASE_URL: postgresql://user:pass@db:5432/mydb\n    depends_on: [db]\n  db:\n    image: postgres:15\n    volumes:\n      - pgdata:/var/lib/postgresql/data\nvolumes:\n  pgdata:\n\n# Commands:\n# docker compose up -d\n# docker compose logs -f app' },
      { type: 'tip', t: '<strong>Always use volumes for database data.</strong> Without a volume, all data disappears when the container stops.' },
      { type: 'h2', t: '🔑 Key Points — Day 47–48' },
      { type: 'keypoints', items: [
        '<strong>Image = template. Container = running instance</strong> — like class vs object',
        '<strong>Each Dockerfile line is a layer</strong> — copy package.json before code for caching',
        '<strong>-p 3000:3000</strong> maps host port to container port (left = host, right = container)',
        '<strong>docker-compose</strong> orchestrates multiple containers as one unit',
        '<strong>Volumes persist data</strong> outside the container — always use for databases',
      ]},
      { type: 'quiz', qs: [
        'What is the difference between a Docker image and a container?',
        'Why do we COPY package.json before COPY . . in a Dockerfile?',
        'What does docker run -p 3000:3000 mean?',
        'What problem do Docker volumes solve?',
      ]},
    ],
  },
  p3d2: {
    phase: 'Phase 4 — DevOps + Cloud',
    day:   'Day 49–55 — CI/CD + Terraform + K8s',
    sections: [
      { type: 'h2', t: 'GitHub Actions CI/CD' },
      { type: 'code', t: '# .github/workflows/ci.yml\nname: CI/CD Pipeline\non:\n  push:\n    branches: [main]\n  pull_request:\n    branches: [main]\n\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: "20", cache: "npm" }\n      - run: npm ci\n      - run: npx tsc --noEmit\n      - run: npm run lint\n      - run: npm test\n      - run: npm run build\n      - name: Deploy to S3\n        if: github.ref == "refs/heads/main"\n        env:\n          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}\n        run: aws s3 sync ./dist s3://my-bucket' },
      { type: 'warn', t: '<strong>Never put API keys in workflow files.</strong> Use GitHub → Settings → Secrets. Reference with ${{ secrets.MY_SECRET }}.' },
      { type: 'h2', t: '🔑 Key Points — CI/CD + Cloud' },
      { type: 'keypoints', items: [
        '<strong>CI</strong> = auto test/build on every push. <strong>CD</strong> = auto deploy on passing builds',
        '<strong>GitHub Actions runners</strong> are VMs GitHub provides free — ubuntu-latest, etc.',
        '<strong>Secrets</strong> are encrypted — never hardcode API keys in workflow YAML',
        '<strong>Terraform</strong> defines infrastructure as code — version-controlled, repeatable',
        '<strong>K8s Pod</strong> = one or more containers. <strong>Deployment</strong> = manages pod lifecycle',
      ]},
      { type: 'quiz', qs: [
        'What is the difference between CI and CD?',
        'How do you safely store and use API keys in GitHub Actions?',
        'What is terraform state and why is it sensitive?',
        'Pod vs Deployment vs Service — what does each do?',
      ]},
    ],
  },
  p4d0: {
    phase: 'Phase 5 — Portfolio Projects',
    day:   'Days 56–65 — Building Portfolio Projects',
    sections: [
      { type: 'h2', t: 'Why Portfolio Beats Resume' },
      { type: 'p', t: '3 deployed projects proves you can build real things. A recruiter can click a link and see it working. That is infinitely more convincing than "I know React" on a resume.' },
      { type: 'h2', t: 'Project 1 — SaaS Dashboard' },
      { type: 'code', t: 'Stack: Next.js 14 + TypeScript + Tailwind + Recharts\n       + NextAuth + Prisma + PostgreSQL\nDeploy: Vercel + Supabase\n\nImpressive features:\n• Google OAuth login (not a fake button)\n• Interactive charts with real data\n• Date range filter that updates charts\n• CSV export\n• Skeleton loading screens\n• Dark mode toggle' },
      { type: 'h2', t: 'Project 2 — Real-time Chat' },
      { type: 'code', t: 'Stack: Next.js + Pusher + Zustand + Prisma + Docker\nDeploy: EC2 (shows DevOps skills)\n\nImpressive features:\n• Real-time messages (WebSockets)\n• Typing indicator\n• Online users list\n• Message history in DB\n• Dockerized — README shows docker compose up' },
      { type: 'h2', t: 'Project 3 — Portfolio Site' },
      { type: 'code', t: 'Sections: Hero, About, Tech Stack, Projects, Contact\nDeploy: Vercel + custom domain (~₹500/yr)\n\nLighthouse scores must hit:\n• Performance: 90+\n• Accessibility: 95+\n• Best Practices: 95+\n• SEO: 95+' },
      { type: 'tip', t: '<strong>Your portfolio site itself IS a project.</strong> If it looks generic or template-based, it signals you can\'t design. Spend real time on the UI — unique fonts, smooth animations, clear personal brand.' },
      { type: 'h2', t: '🔑 Key Points — Portfolio Phase' },
      { type: 'keypoints', items: [
        '<strong>Deploy everything</strong> — an undeployed project does not exist to a recruiter',
        '<strong>Each project shows different skills</strong> — dashboard (data/charts), chat (WebSockets), portfolio (design)',
        '<strong>README is as important as code</strong> — include screenshots, tech stack, setup instructions, live link',
        '<strong>Lighthouse 90+</strong> shows you care about performance and accessibility',
        '<strong>Commit every day</strong> — GitHub contribution graph is visible on your profile',
        '<strong>Explain every decision</strong> — why Next.js, why Prisma, what challenges you faced',
      ]},
      { type: 'quiz', qs: [
        'A recruiter asks "why Zustand over Redux?" — what is your answer?',
        'What is a Lighthouse score and why does it matter to employers?',
        'Walk through your dashboard project architecture — server vs client rendering?',
        'What would you do differently if building Project 1 again?',
      ]},
    ],
  },
  p5d0: {
    phase: 'Phase 6 — Interview Prep',
    day:   'Days 66–80 — Interview Preparation',
    sections: [
      { type: 'h2', t: 'Top JavaScript Interview Questions' },
      { type: 'code', t: '// Q: What is a closure?\nfunction makeCounter() {\n  let count = 0;          // closed over\n  return () => ++count;   // inner fn remembers count\n}\nconst c = makeCounter();\nc(); // 1 — count is private, can\'t be accessed outside\n\n// Q: Implement debounce\nfunction debounce(fn: Function, delay: number) {\n  let timer: ReturnType<typeof setTimeout>;\n  return (...args: unknown[]) => {\n    clearTimeout(timer);\n    timer = setTimeout(() => fn(...args), delay);\n  };\n}\n\n// Q: Event loop\nconsole.log("1");              // sync\nsetTimeout(() => console.log("2"), 0); // async — runs last\nconsole.log("3");              // sync\n// Output: 1, 3, 2' },
      { type: 'h2', t: 'React Interview Questions' },
      { type: 'code', t: '// Q: When does a component re-render?\n// 1. Its own state changes\n// 2. Its props change\n// 3. Parent re-renders\n// 4. useContext value changes\n\n// Q: useMemo vs useCallback\nconst value = useMemo(\n  () => expensive(a, b), [a, b]  // memoizes a VALUE\n);\nconst fn = useCallback(\n  () => doThing(a, b), [a, b]    // memoizes a FUNCTION\n);' },
      { type: 'h2', t: 'Behavioral — STAR Method' },
      { type: 'code', t: '// Q: Why switching careers at 33?\n// WRONG: "I want more money" / "Job is boring"\n// RIGHT:\n// S: Spent X years in previous field\n// T: Wanted to build scalable products\n// A: Committed 80 days of full-time learning —\n//    HTML → TypeScript → React → Docker → AWS\n//    Built 3 deployed projects\n// R: Job-ready with portfolio and proven skills' },
      { type: 'h2', t: 'Salary — Hyderabad Market 2025' },
      { type: 'code', t: 'Junior Frontend (0-1 yr) — Hyderabad:\n• Service cos (TCS, Infosys, Wipro): ₹3.5–5 LPA\n• Mid-size product cos: ₹6–9 LPA\n• Startups (Series A/B): ₹7–12 LPA\n• Remote international: ₹12–20 LPA\n\nNegotiation:\n• Never give current salary first — ask for their budget\n• Give range: "8 to 10 LPA based on role"\n• React + TypeScript + DevOps justifies the higher end' },
      { type: 'tip', t: '<strong>"Why career change at 33?" is your BEST story, not your weakness.</strong> Deliberate decision, full commitment, real projects. That is discipline — companies value this.' },
      { type: 'h2', t: '🔑 Key Points — Interview Phase' },
      { type: 'keypoints', items: [
        '<strong>Closure</strong> = inner function retaining access to outer scope after outer has returned',
        '<strong>Debounce</strong> delays execution until after a pause — implement from scratch in interviews',
        '<strong>useCallback</strong> memoizes functions, <strong>useMemo</strong> memoizes values',
        '<strong>Re-render triggers:</strong> state change, prop change, parent re-render, context change',
        '<strong>Always cleanup useEffect subscriptions</strong> — prevents memory leaks',
        '<strong>STAR method</strong> for every behavioral question — structure prevents rambling',
        '<strong>Your career change story is a strength narrative</strong> — deliberate, committed, delivered',
      ]},
      { type: 'quiz', qs: [
        'Implement debounce from scratch. Explain why it\'s needed for search inputs.',
        'What is a closure? Write a real-world example.',
        'A component re-renders 50 times/second on a slider input. How do you fix it?',
        'Walk through your most complex project — hardest challenge and how you solved it.',
      ]},
    ],
  },
};
